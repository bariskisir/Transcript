/**
 * Enumerates every IPC channel exposed by the desktop application.
 */

export enum IpcChannel {
  AppBootstrap = 'app:bootstrap',
  SettingsSave = 'settings:save',
  CredentialsSave = 'credentials:save',
  CredentialsGet = 'credentials:get',
  CredentialsDelete = 'credentials:delete',
  CredentialsBalance = 'credentials:balance',
  DeepgramModels = 'deepgram:models',
  OpenRouterModels = 'openrouter:models',
  LocalModels = 'local:models',
  LocalModelsRescan = 'local:models-rescan',
  LocalModelDownload = 'local:model-download',
  LocalModelDownloadCancel = 'local:model-download-cancel',
  LocalModelDelete = 'local:model-delete',
  LocalModelSelect = 'local:model-select',
  LocalModelEject = 'local:model-eject',
  LocalModelsOpenDirectory = 'local:models-open-directory',
  SessionStart = 'session:start',
  SessionStop = 'session:stop',
  AudioChunk = 'audio:chunk',
  SessionGet = 'session:get',
  SessionCreate = 'session:create',
  SessionRename = 'session:rename',
  SessionDelete = 'session:delete',
  SessionTranslate = 'session:translate',
  SessionExport = 'session:export',
  WindowAlwaysOnTop = 'window:always-on-top',
  WindowMinimize = 'window:minimize',
  WindowToggleMaximize = 'window:toggle-maximize',
  WindowClose = 'window:close',
  WindowIsMaximized = 'window:is-maximized',
  ThemeSet = 'theme:set',
  ShellOpenExternal = 'shell:open-external',
  LogsOpenDirectory = 'logs:open-directory',
  LogWrite = 'logs:write',
  UpdatesCheck = 'updates:check',
  UpdatesInstall = 'updates:install',
  SessionState = 'event:session-state',
  TranscriptResult = 'event:transcript-result',
  TranslationResult = 'event:translation-result',
  AppError = 'event:error',
  UpdateState = 'event:update-state',
  LocalModelOperation = 'event:local-model-operation',
  LocalEngineState = 'event:local-engine-state',
  LocalModelsChanged = 'event:local-models-changed',
  WindowMaximizedChanged = 'event:window-maximized-changed',
  SettingsOpenRequested = 'event:settings-open-requested',
}
